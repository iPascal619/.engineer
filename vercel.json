{
  "buildCommand": "npm install --prefix ./apps/landing && npm run build --prefix ./apps/landing",
  "outputDirectory": "apps/landing/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
